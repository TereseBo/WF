name: main-branch
on: 
    push:
        branches:
            - main

jobs:
    Echo-main:
        runs-on: ubuntu-latest
        steps:
            - name: Echo main
              run: echo 'branch main'
